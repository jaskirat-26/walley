const user = require('../models/user')

const loginUser = (req, res) => {
    try{

    }catch (err){
        console.log(err);
    }
}

const index = (req,res) => {
    try{

    }catch(err){
        console.log(err);
    }
}


module.exports = {
    loginUser,
    index
}